importC <stdio.h>

func main() -> int {
    entero values[3];
    values[0] = 10;
    values[1] = 20;
    values[2] = 12;
    struct Box {
        entero values[2];
    };
    Box box;
    box.values[0] = values[1];
    C.printf("%d %d\n", values[0], box.values[0]);
    return 0;
}
